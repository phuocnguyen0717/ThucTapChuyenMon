<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nguyenquangphuoc.thuctapchuyenmon_delivery">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".notications.OrderStatus"></activity>
        <activity android:name=".notications.Cart" />
        <activity android:name=".notications.FoodList" />
        <activity android:name=".notications.FoodDetail" />
        <activity
            android:name=".notications.Home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".notications.SignUpActivity" />
        <activity android:name=".notications.SignInActivity" />
        <activity android:name=".notications.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".notications.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>